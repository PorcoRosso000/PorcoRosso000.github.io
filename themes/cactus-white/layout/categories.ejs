<div class="categories">
  <h1><%= page.title %></h1>
  <ul>
    <% site.categories.sort('name').each(function(category){ %>
      <li>
        <a href="<%= url_for(category.path) %>"><%= category.name %> (<%= category.length %>)</a>
      </li>
    <% }) %>
  </ul>
</div>

