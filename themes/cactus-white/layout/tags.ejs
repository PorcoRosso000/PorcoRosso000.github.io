<div class="tags">
  <h1><%= page.title %></h1>
  <ul>
    <% site.tags.sort('name').each(function(tag){ %>
      <li>
        <a href="<%= url_for(tag.path) %>"><%= tag.name %> (<%= tag.length %>)</a>
      </li>
    <% }) %>
  </ul>
</div>
